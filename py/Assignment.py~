from datetime import date
from Grade import Grade

class Assignment:

  def __init__(self, typ="HW", index=0, name="the beginning", due=date.today(), grade=Grade(), notes="this is the first assignment" ):
    self._type = typ
    self._index = index
    self._name = name
    self._due = due
    self._grade = grade
    self._notes = notes
    
  def due(self,x=None):
    if x == None:
      return  self._due
    else:
      self._due=x

  def type(self,x=None):
    if x == None:
      return  self._type
    else:
      self._type=x
      
  def index(self,x=None):
    if x == None:
      return  self._index
    else:
      self._index=x
      
  def grade(self,x=None):
    if x == None:
      return  self._grade
    else:
      self._grade=x
      
  def name(self,x=None):
    if x == None:
      return  self._name
    else:
      self._name=x
      
  def notes(self,x=None):
    if x == None:
      return  self._notes
    else:
      self._comments=x
      
